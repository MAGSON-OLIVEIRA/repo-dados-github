name: deploy fake
on: 
    workflow_call:
        inputs:
            ambiente:
                type: string
                default: "desenvolvimento"
                required: true
                description: "amb deploy"
jobs:
    deploy-kubernetes:
        runs-on: ubuntu-latest
        steps:
            - name: Deploy no ambiente
              run: echo "Executando o deploy no ambiente de ${{ inputs.ambiente }}"

